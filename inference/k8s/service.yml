apiVersion: v1
kind: Service
metadata:
  namespace: server
  name: flask-inference-server
spec:
  selector:
    app: flask-inference-server
  ports:
    - port: 80
      targetPort: http
  type: LoadBalancer
